# 兼容性测试(dsh-shell-select)

分层定义照仓库惯例:L2 统一底线(CI compat job 自动),L3 功能面层人工逐格执行。

## L3 功能面矩阵

宿主版本基准:0.1.5-rc.2(开发基准)。执行环境:Windows + 已挂 profile。

| # | 场景 | 步骤 | 预期 |
|---|------|------|------|
| 1 | 启动加载 | 挂载后启动 dsh web | 页面无 `Failed to load plugins`;diagnostics findings 0;工具列表出现 `shell`、无 `pwsh` |
| 2 | 默认执行 | 会话内让模型跑 `shell` 工具(缺省 shell 参数) | 走 default 条目 argv(pwsh 形含编码前缀);`[exit code: N]` 标记正常 |
| 3 | 按名选择 | 工具传 `shell: git-bash` | argv 呈 `bash -c` 形;POSIX 命令可跑 |
| 4 | 自动探测 | 清空 path | pwsh/cmd/git-bash 均解析到真实路径(设置页「可用」徽标) |
| 5 | 自定义路径 | 指向第三方可执行 + args 模板 | 命令经模板执行;`{command}` 占位替换正确;条目 env/distro 保存回读一致,spawn env 含条目键 |
| 6 | 后台任务 | `run_in_background: true` | 返回 job id;job_output 增量读取;job_kill 可停 |
| 7 | 设置热更 | 设置页改默认客户端并保存 | 新工具调用即用新默认;工具描述更新(shell 枚举/文案) |
| 8 | 坏路径 | 条目 path 指向不存在文件 | 工具报配置指引错误,非 spawn ENOENT |
| 9 | 沙箱拒绝 | read-only 模式跑写操作 | `[sandbox: file access denied under read-only mode]` 标记 + 升权提示;升权后可执行 |
| 10 | 市场开关死态自愈 | 市场里关闭本插件 | 日志出现代挂记录;`pwsh` 工具恢复可用;重新开启后代挂卸载、`shell` 工具回归 |
| 11 | 卸载复位 | `dsh plugin` 移除本包并重启 | 官方 `pwsh` 工具复位;`shell-select` 设置节消失 |
| 12 | 升级回归 | 升级 dsh 本体后跑 `node scripts/smoke-load.mjs` + 本包测试 | 加载无命名导出缺失;测试全绿 |
| 13 | bash 登录链 | bash 条目 `login: true`,跑 `shopt -q login_shell; echo $?` | 输出 LOGIN_YES(0);`-lc` 生效;配置→entryFor→argv 全链 |
| 14 | wsl 发行版 | wsl 条目配 `distro`,实机发行版下跑命令 | argv 呈 `-d <distro> --exec` 形;指定发行版执行正确 |
| 15 | 官方行卡片 | 死态窗口(guard 代挂)会话内跑官方 `pwsh` 工具 | 调用行走增强卡(折行/复制/退出码 pill),非通用卡 |
| 16 | deny 拦截 | `deny: ['format\\s']`,跑 `format c: /q` | 拒绝执行,输出含 `[blocked by shell-select: matches deny pattern …]`;spawn 未发生 |
| 17 | allow 豁免 | 同上 + `allow: ['rm -rf .*node_modules']`,跑 `rm -rf ./node_modules` | 豁免放行正常执行;`rm -rf C:\Windows` 仍拒绝 |
| 18 | 名单配置往返 | 设置页拒绝/豁免文本域保存 | yaml deny/allow 落盘回读;非法正则保存被校验拦截 |

## 已知边界

- POSIX 部署:本包全部行停用,功能面 1-11 不适用(预期:官方 bash 链照常)。
- headless(无 webServer):设置页路由跳过,settings.yaml 手工配置,能力不受影响。
- guard 代挂窗口内的官方工具为官方配置(官方 `shell` 节),本包设置页改动的 shell 清单不生效,属预期。
- 死态窗口(用户 patch 禁主行):agent preset 注入的官方 tool-pwsh 行因 `waiting for shell` 拒挂(cordis 服务按 fiber 树解析,guard 代挂对他树 preset scope 不可见),死态窗口新会话无 shell 工具属预期;guard 价值边界为救活 boot 组合与 host 面。
- pwsh 客户端 stderr 在非 UTF-8 console 代码页下中文可呈乱码(编码前缀只覆盖 stdout 面);执行与 exit code 不受影响。
- 受限模式 × Cygwin 系运行时(git-bash/msys2):confine 包装后 Cygwin mmap 在受限令牌下崩溃(`CreateFileMapping … fatal error`,exit 256)。失败可见、错误自解释,模型可换客户端或升权重跑;危险模式与 pwsh/cmd/wsl 不受影响。属 Cygwin 运行时限制,不做路径预判拦截。
- msys2 login 形:`-lc` 只把 /usr/bin 注入 PATH;/mingw64/bin 需 MSYSTEM=MINGW64 环境变量(条目 env 配置 `MSYSTEM: MINGW64`,或 OS 级全局环境继承——args 模板管 argv 不管 env)。未设时 gcc 类工具 command not found 属预期。
- wsl 形在未安装发行版的机器上:wsl.exe 输出 UTF-16 帮助文本呈乱码;正常发行版下 bash 输出走管道为 UTF-8 不受影响。

## 执行记录

- 2026-09-20:12 格全过(port 9192,详见 `docs/progress/shell-select-verify-9191.md` 第二轮)。格 9 以 workspace-write 等价覆盖;格 10 死态自愈见第一轮;格 12 冒烟 + 全套测试 75 pass。过程中修复 cordis 服务代理 × `#` 私有字段阻断缺陷(补齐官方 seam `run`/`start`/`sandboxMode`),详见执行记录与 `test/executor-seam.test.mjs`。
- 2026-09-21:竞品优化轮新增场景实测(port 9192,全套测试 92 pass):
  - login 链:bash 条目 `login: true` → 实跑 `shopt -q login_shell` 返回 LOGIN_YES(配置 → entryFor 透传 → argv `-lc` 全链;过程中发现并修复 entryFor 丢 login 字段缺陷,补 entryFor 级测试)。
  - bash 探测修正:msys2 锚位候选 + PATH 扫描排除 SystemRoot(System32\bash.exe 为 WSL 转发器);msys2.exe 静默失败(管道 stdio exit 0 零字节)永不入候选。
  - wsl WSLENV:DSH_* 追加透传单测锁定;实机 wsl 无发行版,wsl.exe 回 UTF-16 帮助文本呈乱码(见已知边界)。
  - 会话卡:tool.call.toolview key 'shell' 卡实测——前台成功(done,无 pill)、前台失败(退出码 256 pill)、后台 ack(generic 回退行)、溢出、客户端名徽标(git-bash)、复制按钮、cwd 段全数正确;修复组件函数直调引发的 React #310(直调组件 hooks 计入外层渲染)与 callView/resultView 幻觉字段(官方 block 无此二者,数据源应为 argsRaw + 结果文本)。
  - 受限模式 × git-bash 实测:confine 包装后 Cygwin 运行时 `CreateFileMapping … fatal error`(exit 256),失败可见且错误自解释;同命令完全权限下成功(MINGW64)。与 dsh-bash-terminal-ts 观察一致,记录为已知边界,不做 Cygwin 预判拦截(路径判定脆弱,模型可从错误文本自行换客户端/升权)。
- 2026-09-21 竞品对标轮(env/distro/三 key 卡,对标 dsh-bash-terminal-ts 与 dsh-shell-card-plus,全套测试 113 pass):
  - 条目级 env:三层优先级(内置覆盖集 < 条目 env < 调用方)与 wsl WSLENV 联动由 env.test.mjs 锁定;设置页每行 K=V 编辑往返(client-id 守卫扩展)。
  - wsl distro:`-d` argv 形、模板条目忽略、schema 默认补齐由 config-entry.test.mjs 锁定。
  - 官方工具行卡片:key 扩为 shell/pwsh/bash 三支(死态窗口代挂的官方 tool-pwsh 调用行获同款增强卡);persistent 形(无 description)running/settled 均回退 generic,shellCardModel 行为测试(LOGIC 段提取,client-card.test.mjs)锁定。
  - 漂移守卫:client KINDS/KIND_LABELS 与 host KINDS 同源断言(client-drift.test.mjs)。
  - 交互式 PTY 终端经评审不做:独立子系统(五动作工具面 + node-pty 原生依赖),与 ctx.shell 接缝无关,列入路线图后续立项。
- 2026-09-21 命令黑白名单轮(deny/allow,全套测试 132 pass;竞品确认无此功能):
  - 测试面:matchDeny 纯函数 6 场景(命中/豁免/大小写/空配置/坏正则/多行)、executor 双入口拦截(stub spawn 断言未发生)、tool 双分支转译源级守卫、updateConfig 往返断言。
  - 实机配置链:9192 设置页拒绝名单文本域保存 → settings.yaml `deny: [deny-probe-format\s]` 落盘确认(端到端);验后还原出厂。
  - 实机工具链未完成:9192 新会话经页面/RPC 双路均未产生轮次(auto-free 排队未起转,环境摩擦非产品缺陷);工具链语义由测试覆盖兜底,拦截标记走官方 stderr 渲染通道无自定义渲染面。
  - 边界记录:整文本正则不挡间接包装(编码/嵌套壳),定位防误触护栏而非安全边界(README 已注明);坏正则条目容错跳过。
